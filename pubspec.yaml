name: taxi_driver
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.20.2
  cupertino_icons: ^1.0.8
  http: ^1.2.1
  json_annotation: ^4.9.0
  shared_preferences: ^2.3.2
  flutter_background_service: ^5.1.0
  flutter_background_service_android: ^6.3.1
  flutter_secure_storage: ^10.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  json_serializable: ^6.8.0
  build_runner: ^2.4.11
  flutter_launcher_icons: ^0.14.4

# ✅ CORRECT NAME (TOP LEVEL)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/taxi_logo.png"
  adaptive_icon_background: "#FFFFFF"
  adaptive_icon_foreground: "assets/taxi_logo.png"

flutter:
  uses-material-design: true

  assets:
    - assets/taxi_logo.png
